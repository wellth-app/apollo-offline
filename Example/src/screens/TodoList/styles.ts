import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {},
  list: { flex: 1 },
  row: {
    height: 25,
    justifyContent: "center",
    // alignItems: "center",
    flex: 1,
  },
});
